/*
 * 1. create a project in console.firebase.google.com
 * 2. npm install firebase 
 * 3. create firebase.init.js and import getAuth to export auth
 * 4. firebase settings > Authentication > enable Email and password auth
 * 5. create Login, Signup component, setup route
 * 6. attach form field handler and form submit handler
 * 7. npm install --save react-firebase-hooks
 * 8. useCreateUserWithEmailAndPassword(auth); from react firebase hooks
 * 9. if user created redirected to the expected page via navigate
 * 10. signInWithEmailAndPassword(email, password) for signIn
 */



/**
 * hosting steps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 */